# Risk Register

**Status:** Session output 2026-08-17 · R14 added 2026-08-18; R2/R5/R8/R11/R14 annotated and
**R6 marked realised** after S6; **R9 annotated and R1 marked realised** after S9's first frame;
**R15–R18 added 2026-08-19** with the universe phase (ADR-016), none yet validated;
**R19–R21 added and R10/R17/R18 annotated 2026-08-19** with the scaling baseline
([ADR-018](ADR/ADR-018-scaling-baseline.md)). Top
technical risks, each with mitigation already wired into the ADRs/build order (referenced),
plus its early-validation point. Review at each milestone (M0/M1/MVP, then W0/W1/W2).

A risk that has actually fired is marked **🔥 realised** with what happened. That is the
useful part of a register: not the list of things that might go wrong, but the record of which
guesses were right.

| # | Risk | Impact | Likelihood | Mitigation (designed-in) | Early validation |
|---|---|---|---|---|---|
| R1 | 🔥 **realised (three times, at S9)** — **Isometric readability** — flat-shaded fleets on a dark plane become unreadable blobs; depth/occlusion confusion sinks the core fantasy | High | Med | Planar sim kills depth ambiguity at the root (ADR-001); ortho 30° + world-space 2:1 rings + screen-floor clamps per corpus scaling law (ADR-006); icon channels/de-clutter ladder reserved | S5 visual checkpoint vs prints; S8 occlusion rules; playtest at min/max zoom each slice after. **Standing as of S9, and this is the one row where the mitigation has outrun the validation.** Three slices of overlay now exist — rings, gauge bars, order footprints, station ticks, the ring/bar depth split, the decal bias pair — and **not one frame of it has been on a screen.** Everything device-free about it is tested; everything that needs a GPU is a guess. S9 put a number on what that costs: two `OverlayTuning` colours had been written `0xAARRGGBB` and read `R8G8B8A8_UNORM` since S8, so the selection ring rendered amber and the shield bar blue, and nothing caught it — a swapped colour and an unrun frame are the same blind spot. The backlog here is not "playtest at min/max zoom" any more, it is **run the frame at all**: the ring bias (`-100`, slope-scaled `-1`) and the claim that bars never occlude are both unverified. **Then the frame was run.** It closed the two oldest rows outright — interpolated motion reads as motion at 144 Hz over 20 Hz snapshots (S7), and **rings occlude behind a Carrier hull while bars never do** (S8), which is `overlay-pass.png`'s rule holding and which promotes the decal bias pair from a textbook guess to a measurement. It also **found three defects in one sitting** — the swapped colours above, a ring whose *thickness scaled with its own radius* (a normalised floor is a fraction of the radius, so past forty pixels it overtook `fwidth` and a 700-pixel footprint became a forty-pixel band), and a puck sized to *circumscribe* the formation, which for a Line is half its length and put an ellipse across the viewport. Every device-free test passed throughout all three, because each defect lives in the product of two pieces of arithmetic that are individually correct. **The register's value here was the same as R6's: it predicted where to point an instrument, and the instrument is a person looking at a frame** — which both retired criteria no number could settle and caught defects no number did. What is still open is narrower than the row started: the visual checkpoint against the prints, legibility at min and max zoom, and a second look at the overlay after the two size fixes. |
| R2 | **Determinism erosion** — accidental clock/RNG/iteration-order leaks make replays lie and (later) prediction/desync work impossible | High | Med | Same-binary replay determinism is a *tested invariant*, not a goal: double-run hash suite from S6, `selfTest` gate in CI (ADR-005); single-writer + owner asserts (ADR-007); /fp rules; validation-on-quantised-inputs parity rule | S6 harness lands before first networked ship (S7); any red replay test blocks merge. *Standing as of S6: the double-run suite is live — 1,000 ticks, fifty bit-identical checkpoints, with a control that changes one order by one metre and requires every later checkpoint to notice. The mitigation is no longer "asserted in review": **CI fails GameLogic for including `<chrono>`/`<random>`/`Clock.h`, calling `rand`, or naming `std::chrono`** — the leaks this risk is about are now caught by the build rather than by the replay suite eventually going red for an unexplained reason.* |
| R3 | **msquic integration friction** — Schannel-flavour cert provisioning (no PEM/PKCS12), callback threading, QUIC-TLS OS dependency (Win11/Server 2022+) | Med | Med | QUIC-shaped `Transport` from day one so only the impl is at risk (ADR-003); known-good path pre-chosen (`CERTIFICATE_CONTEXT` + in-memory self-signed, ALPN `opf/1`, `NO_CERTIFICATE_VALIDATION` on loopback); UDP fallback is permanent *(superseded at S13 — see disposition)*; OpenSSL package flavour identified as the swap (flagged in Dependency Map §5 for owner sign-off) | S13 spike is *scheduled*, pre-MVP, with explicit exit criteria; datagram cap enforced from S4 so no payload surprises. *Resolved at S13: the spike landed in a day, all accept criteria met, and the disposition is **stay Schannel** — the OpenSSL flavour remains a listed swap, not a need. The pre-chosen path held with one sharpening: `CERTIFICATE_CONTEXT` with an in-memory self-signed certificate worked against Schannel first try, but the private key has to be a **persisted** CNG key named by the certificate's provider info — Schannel resolves the key through that container, and an ephemeral key has no container to name (the key is deleted with the transport, so nothing lingers). The real friction was elsewhere, and an existing test caught it: `ConnectionShutdown` abandons queued stream data, so the server's build-mismatch refusal was discarded before it left the machine — `Close()` now shuts the control stream down gracefully and defers the connection close until msquic confirms delivery. One measurement finding: msquic's smoothed RTT reads ~2 ms on an idle loopback link purely from deliberate ack batching (~25 ms max ack delay against the OS timer), so the accept's "added latency" is measured on **min RTT: 0.305 ms**, Debug build, against the 1 ms gate. The mitigation column's "UDP fallback is permanent" fell to the owner's QUIC-only directive; `UdpTransport` is deleted and the threading contract absorbed msquic's worker callbacks exactly as ADR-007 §3 said it would.* |
| R4 | **DX12 boilerplate burden** — device/swapchain/sync/upload plumbing consumes weeks before gameplay | Med | High | Scope fenced to 5 PSOs, one queue, no compute, no bindless, no MSAA initially (ADR-006); glyph atlas instead of D3D11On12/D2D interop (largest boilerplate item deleted); debug layer on from S1 | S1 and S5 are the first slices — burn-down is visible in week one, not month three. *Closed out as fenced: the plumbing never grew past the fence, and the one deferral came back on schedule — S14 landed the 4× MSAA offscreen + resolve as the listed polish slice, still 5 PSOs (the UI pipeline stayed single-sampled), still one queue, still no compute* |
| R5 | **Loopback UDP is not a function call** — socket-buffer drops/stalls under load or debugger pauses look like heisenbugs | Med | Med | Contract forbids assuming zero loss (ADR-003); full snapshots are idempotent (ADR-004); interp→extrap≤250 ms→STALE degrades gracefully (ADR-002); receive buffers sized at init; drop counters in telemetry | S7 induced 400 ms stall test; drop counters visible in S14 strip. *Partly realised at S4: Windows reports a send to an absent peer as `WSAECONNRESET` on the **receive** call, which stopped the drain loop for that poll and let one dead peer starve every other connection. The drain now tolerates it and continues.* |
| R6 | 🔥 **realised (twice, at S6)** — **Formation/steering feel** — servo oscillation, station fighting, straggler deadlocks make the one MVP mechanic feel bad | Med | Med | Arrival-slowdown steering with class clamps, stations non-overlapping by construction, leg completion tolerance + straggler timeout (ADR-005); envelope tests pin turn radius/overshoot | S6 envelope suite; S10 "preview equals outcome" test; tuning constants isolated in `ShipClassTable`. *As of S9 the "preview equals outcome" claim is structural rather than a test to write: the client's footprint calls `SolveFormation` through the seam — the same function, on the same quantised leg, with the same sort by ascending `ShipId` — so a preview that differed from the outcome would need the two to disagree about their own inputs. What S10 still has to prove is that the arrangement **fits**: the print's own open question is what the puck should do when a station lands inside a gate or another fleet.* **Both failures this risk predicted fired in S6, and neither was visible in the source.** *(1)* A hull's turn radius at cruise is `speed / turnRate` — 477 m for a Battleship — so a target sixty metres astern sits inside the circle it can turn on: the ship held its speed, swept past, and came round again, **three laps and a hundred seconds of simulated time to travel sixty metres.** Fixed by bounding speed at `omega·distance/(2·error)`, chosen by measuring four candidate limits across five hulls and five geometries. *(2)* The textbook braking curve `sqrt(2as)` is achievable only in continuous time; stepped at 20 Hz a ship sits above it, covers more ground per tick, and falls further behind — compounding all the way in. An Interceptor arrived 1.2 m out still doing **38 m/s** and settled 5.9 m past its target. Fixed with the discrete solution. **The lesson is the mitigation:** the envelope suite did not prevent either bug, it *found* both — the register's value here was predicting where to point a harness. |
| R7 | **Scope creep from the corpus** — the prints describe a shipped game (strategic map, alerts taxonomy, abilities); building toward pictures instead of the MVP | Med | High | Every ADR carries an explicit MVP-subset + reserved-seam table; overview's omissions table is the contract; new surfaces require a slice + owner ack | Build order is the whip — anything not in S1–S14 is post-MVP by definition |
| R8 | **Float precision at world scale** — future galaxy-scale coordinates break float32 sim/render | Low (MVP) | Med | Resolved structurally: exact `int64` metre universe plane with grids anchored at `UniversePos`, sim/wire stay local float32/cm (ADR-009 §2). Residual risk is *discipline* — code subtracting distant universe coordinates, or leaking `UniversePos` into per-tick sim math | Anchor+local reconstruction property test in `GameLogicTests` (landed early, with S5b: 121 positions plus the ends of the int64 plane, where an unguarded subtraction wraps a distant position into a false accept). *As of S6 the grep-able rule is a **CI step**: `World`, `ShipClass` and `WorldHash` fail the build if they name a `UniversePos` or `UniverseDef`.* |
| R9 | **Text/HUD scope** — HUD needs (roster, bars, toasts, ETAs) balloon into a UI framework | Med | Med | Atlas ASCII + quads only, layouts hardcoded to print zones, UI-scale multiplier the only flexibility (ADR-006); no i18n/shaping in MVP | S11 accept criteria; any widget beyond the prints' zones is a flag. *As of S9 the Ui pass had a queue forming and it was worth naming so S11 was scoped against it rather than surprised by it: the selection drag rectangle (S8), the bounce toast's reason string (S9), the ghost's dashed lane and per-leg ETA labels (S9), and the NET/tick readouts. All four are screen-space quads or text, which is exactly what the pass is; none of them was a widget, and by S11c all four are drawn. The queue emptied without growing, which is the outcome this row was watching for. **One of them cost a primitive rather than a layout:** the lane needed an oriented quad, because the pass could only place a rect and a lane at 45° is not one (ADR-006 §8c). That is the shape to watch next — not a widget, but the pass's vocabulary growing one entry at a time. `overlay-pass.png`'s mechanism-B list still holds engagement arcs and off-screen indicators, and both are already expressible with what S11c added, which is the test of whether the entry was the right one* |
| R10 | **Tick budget at scale** — 20 Hz fine at 41 ships, unknown at 1,024 replicated + interest mgmt | Low (MVP) | Med (later) | `tickOverrun` + per-stage spans are release telemetry from S3 (ADR-002); snapshot growth path (delta+interest) reserved in wire (ADR-004); sim is SoA + single-writer, parallelisable behind the replay gate | Synthetic 1,024-entity headless soak once S6 lands (cheap, scriptable). *`tickOverrun` is a real telemetry counter as of S2, not a bare atomic, and `Tick`/`Poll` are spans on the `Sim` lane — the budget rows exist before there is a budget to blow.* *Annotated per ADR-018 (A4/A26): the soak splits in two — the **sim half is runnable now** and must run, in Release, before U2 (its number decides the broadphase question); the **wire half is blocked** on the interest/delta design (any world past the snapshot cap refuses to serialise), so the 1,024 figure stays sim-only until D6 of the universe deliverables lands. The budget is now numeric: the 50 ms tick must hold the shard's target grids × entities (ADR-018 D1c).* **🔎 Sim half measured 2026-08-19** (indicative cross-build: clang 18 `-O2`, 2.1 GHz Xeon, real `World::Tick`, converging-crowd worst case; the authoritative MSVC Release number now has a CI leg to come from): 41 ships **0.013 ms**, 256 **0.43 ms**, 512 **2.2 ms**, **1,024 ships 10.6 ms mean / 21.9 ms worst — 21 % of the 50 ms budget**. Quadratic as expected, and the headline is that a capped grid costs about **a fifth of a core, not a whole one**, so ~5 capped grids or ~3,700 MVP-sized ones fit one core at 20 Hz. **The broadphase is therefore not needed before U2** — it is a tuning decision for whenever grids routinely approach the cap, not a structural prerequisite. What this does *not* cover, per NET-5: fan-out, datagram scheduling, and client apply at 1,024 remain untested and untestable until the interest/delta design lands. |

| R11 | **DirectXMath discipline** — an `XMVECTOR` stored in a struct/container (alignment), a missing `XM_CALLCONV`, or an `XM*Est` call sneaking into GameLogic (breaks replay equality) | Med | Med | Storage-vs-computation rule and the `Est` ban are normative (ADR-010 §3, §6); `/arch` pinned solution-wide; `XMVerifyCPUSupport` at startup | Replay suite catches `Est` drift immediately (S6), and **CI now fails GameLogic outright for any `XM*Est(` call** rather than waiting for a replay to go red on one machine and not another. Stored `XMVECTOR`s remain a review item — `World`'s tables are `XMFLOAT2`, loaded and stored per operation |
| R12 | **The JSON parser is load-bearing** — it reads config, universe content, and sound banks, all hand-edited; a silent bug (precision loss past 2⁵³, bad escapes, deep-input crash) corrupts world data or bricks startup | Med | Med | Scope is a strict subset with two documented relaxations; iterative parse with depth cap; exact `int64` path; never throws/asserts on input; fatal on type mismatch and duplicate keys rather than coercing (ADR-012 §C) | The conformance corpus is part of S2b's acceptance, before anything depends on the parser |
| R14 | 🔥 **realised** — **Flat layout + per-project include roots ⇒ silent name shadowing.** With every project folder on the include path and no directory qualification, a header whose name matches a CRT or STL one wins the search, and a duplicate file name across two projects resolves to whichever root comes first. Neither produces a diagnostic pointing at the cause | Med | **High** (already happened twice) | ADR-013 §3 makes names unique repo-wide and §3a extends that to the CRT/STL/SDK case-insensitively; the registry in §3 is filled in *before* a file is created; ADR-013 §4 states plainly that rule 3 carries the weight the qualification would have | **Two CI steps now cover both halves.** The first fails the build on any header whose stem matches a standard one — added after `NeuronCore/Time.h` shadowed `<time.h>` and produced ~24 errors inside `<ctime>` with nothing naming our file (`Assert.h` had the same latent collision; both renamed, to `Clock.h` and `Debug.h`). The second, added with S7's documentation pass, **fails the build on any duplicate file name across the five projects and `Tests/`** — 108 names checked, `pch.h`/`pch.cpp` exempted because they sit at every project root by design and are never included across a boundary. The registry in ADR-013 §3 stays the discipline; the build is now the backstop |
| R13 | **Audio feel under an ortho camera** — listener model wrong ⇒ pans that don't match the screen, zoom that doesn't attenuate; voice-pool thrash at fleet scale | Low | Med | Listener at camera focus raised by zoom, mono emitters, Doppler off (ADR-011 §4–6); pooled voices with priority/distance stealing designed in, not retrofitted | S15 manual check (pan/zoom/stress at 200 ships); math is unit-testable headlessly |
| R15 | **Bake legibility** — the generator's layout defeats the strategic map: even scatter instead of clustered constellations, and the print's hulls and labels stop working (its own stated failure mode) | Med | Med | Cluster separation is a bake-time *invariant*, not an aesthetic hope (ADR-016 §2); constellations authored as clusters with gaps; naming stays `ROOT-N` so labels stay short by construction | U1's invariants suite runs against the committed file; U5's accept is a visual checkpoint against `strategic-map.png` *over the real bake* — the invariant paying off on screen or the slice not closing |
| R16 | **Multi-grid determinism erosion** — transfer-bus ordering, world teardown/recreation, or per-world seeding leaks make the replay contract lie once ships cross grids | High | Med | Transfers apply between ticks in (arrival tick, order id) order; worlds seed PCG32 from (session seed, anchor id); the transfer log joins the replay contract from the first transfer (ADR-016 §4); the `UniversePos`-in-tick-code CI guard extends to new files | U2's registry double-run + teardown/recreate suite lands *before* the first transfer exists; U3a's accept is bit-identity across concurrent warps; any red replay test blocks merge, as ever (R2's regime, widened) |
| R17 | **2,500-system content boot cost** — a multi-megabyte JSON parsed and hashed by both halves every boot; the custom parser has never seen this scale | Med | Med | Parse is iterative with exact int64 (ADR-012); the file is baked once and read many times; per-region file split is the reserved fallback, named before it is needed (ADR-016 §2) | U1's accept **measures** parse+hash on the committed file and records the number; the fallback triggers on a stated threshold (~1 s), not on a feeling. *Annotated per ADR-018 (D11): the number is measured in **Release** — a structural fallback must not trigger on Debug timings; the Release CI leg (A2) exists before U1 so it can be.* |
| R18 | **View-switch and summary wire cost** — grid re-subscription smears stale snapshots into the new view, the interp-refill reads as a jank, or fleet-summary fan-out grows past its budget as fleets multiply | Med | Med | Grid identity in every snapshot header (late datagrams name the wrong grid and are dropped, never blended); the switch is a designed ~200 ms settle over the ~100 ms interp refill (ADR-016 §7); summaries are ~1 Hz and a few dozen bytes per fleet, on the existing datagram budget | U3b's accept: roster-click to smooth motion in under half a second, over the real loopback in `selfTest`; summary bytes get a telemetry counter the day they exist, per R10's regime. *Annotated per ADR-018 (A15): the switch budget is RTT-linear, so the target is stated as **RTT + settle** and re-run with an injected-delay shim — loopback-only numbers do not close the row; the map is the named surface for switches that exceed the settle.* |
| R19 | **The replication cliff** — the full-snapshot cap is 43 records post-T2 against 42 of authored content (margin one); any second commander on a grid, fleet growth past the cap, or a 64-ship undock into an occupied grid makes the server refuse the whole snapshot — a session-killing outage by construction, and the sim cannot backstop it (undock is never refused for clutter, transfers-in have no refusal path) | High | High (the moment grids are shared) | The growth path is designed and owned: the interest/delta ADR (ADR-018 D4, universe deliverable D6) with a fixed scope — ack/baselines, keyframe path, relevance hook, degradation rule, the owned+selected-never-culled guarantee; until it lands, bake/scenario invariants keep authored per-grid population under the cap and **shared grids are gated behind the slice** (U3c runs on disjoint grids) | T2's accept tests the over-cap refusal loudly; the interest/delta slice is triggered by the second-commander milestone, not "later" |
| R20 | **Device removal on remote clients** — TDRs, driver updates, and iGPU/dGPU switches are routine on shipped machines; today any occurrence exits the client via the composition-root handler | Med | High (once clients are remote) | Decided posture (ADR-018 D13): relaunch-and-reconnect is the shipped behaviour for the first remote milestone — the server survives and reconnect is first-class (D5). The invariant that keeps in-session recovery cheap to add is recorded and reviewed: **no session state may hold a device reference**, every device resource keeps a re-runnable Create/Destroy pair | Review item on every slice that adds a device resource; a recovery slice is scheduled only when relaunch stops being acceptable |
| R21 | **Single-process topology vs the shard target** — the MMO decision (ADR-018 D1: hundreds of commanders, one persistent universe) exceeds what one process/one machine can promise forever; grid-to-host assignment, cross-host transfer ordering, and connection handoff are undesigned, and a registry built carelessly would bake single-process assumptions into the replay contract | High | Med | **Mitigated by design 2026-08-19: [ADR-019](ADR/ADR-019-shard-topology.md)** — three roles in one process today, the anchor as placement unit, `(applyTick, hostId, counter)` as a total order needing no coordination, transfers filed at departure so latency is slack rather than a race, and one client connection through a session front door so **the client wire never learns the topology exists**. The registry stays location-transparent (D1a), which world-level parallelism needs anyway — the discipline pays twice. Two findings the ADR turned up while writing: the shard tick needs to be a **stated contract** rather than a convention (`World::Tick(_tick)` already takes the number, but nothing says it is shard-global, and the hash folds it — so a recreate comparison is only meaningful at equal ticks), and transit durations need a `TRANSFER_FLOOR_TICKS` floor | U2's accept carries ADR-019 §6's eight shapes plus the isolation and permuted-tick-order tests (ADR-018 A8); the sizing that motivates it — ~1 core per capped grid, single-digit hosts at the stated target — is checked by A4's soak |

## Standing spikes (cheap, do early, answer one question each)

1. **Loopback heartbeat under debugger** (S4): does breaking in the IDE wedge either side?
   Expected: no — transport tolerates stalls; verifies R5 posture. *Still to run: it needs a
   debugger and a person. The automated half (`selfTest`) passes.*
2. **Replay-hash across Debug/Release** (S6): confirm hashes *differ* across configs is
   acceptable and documented (same-binary scope, ADR-005 §6) — prevents a false alarm later.
   *Still to run: CI builds Debug only, so the two configs have never been compared. The suite
   that would answer it exists as of S6; it needs a Release build and one comparison.*
3. **1,024-instance draw** (after S5): 1,024 `InstanceRecord`s through the opaque pass —
   validates the corpus entity cap costs nothing on the render side (it shouldn't, instanced).
   *Still to run: needs a GPU. The instance path it would exercise has been in the tree since
   S5 and the scene is now built through the `WorldView` seam, so the test is a scenario rather
   than a code change.*
4. **Schannel QUIC availability** (before S13): one-liner msquic `QuicOpen` + listener on the
   dev/CI images — surfaces the OS-dependency question while there's slack, not during S13.
5. **Config-by-directory in CI** (with S2b): confirm a runner can select the self-test
   configuration purely by its working directory — this is the workflow that replaced flags,
   so it is worth exercising once before the build depends on it.
