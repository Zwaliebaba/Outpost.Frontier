// pch.h: precompiled header for NeuronClientTests.
//
// Deliberately bare. These tests cover presentation maths and configuration
// defaults, which need no window, no device and no COM -- so they must not
// drag in the graphics headers just to build.

#ifndef PCH_H
#define PCH_H

#endif // PCH_H
